# Interface, download do YouTube, transcricao com tempos e exportacao .docx
streamlit>=1.36
yt-dlp>=2024.8.6
faster-whisper>=1.0.3
python-docx>=1.1.0

# ffmpeg empacotado, dispensa instalar no sistema
imageio-ffmpeg>=0.5.1

# Certificados do sistema (rede corporativa com proxy que intercepta o TLS)
truststore>=0.9

# Node 22+ para o yt-dlp resolver os desafios de JavaScript do YouTube.
# O nodejs do apt (Debian) e a versao 18, que o yt-dlp ignora por ser antiga
# demais, e ai o download termina em HTTP 403.
nodejs-wheel-binaries>=22
